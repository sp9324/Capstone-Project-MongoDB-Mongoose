<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Task List</title>
    <link rel="stylesheet" type="text/css" href="styles/stylesTasks.css">
</head>

<body>
    <h1>
        <%= listTitle %>
    </h1>
    <a href="/">Home</a>
    <form action="/submit" method="POST" class="item">
        <input type="text" name="task" placeholder="Enter task here!">
        <input type="hidden" name="listTitle" value="<%= listTitle %>">
        <input type="submit" value="OK">
    </form>
    <ul>
        <% tasks.forEach(task=> { %>
            <form action="/delete-task" method="POST">
                <li><input type="checkbox" name="checkbox" value="<%= task._id %>" onChange="this.form.submit()" />
                    <%= task.name %>
                        <input type="hidden" name="listTitle" value="<%= listTitle %>">
                </li>
            </form>
            <% }); %>
    </ul>
    

</body>

</html>